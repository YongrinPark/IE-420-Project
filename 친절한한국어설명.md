# 📊 IE 420 파이널 프로젝트 한국어 설명서

> **Real-Time Portfolio VaR Monitoring System**  
> 쉽게 말하면: *"내 포트폴리오가 하루에 얼마나 손해볼 수 있는지 실시간으로 추적하는 시스템"*

---

## 🧭 이 파일을 먼저 읽어야 하는 이유

영어로 된 `README.md`는 설치 명령어 위주라 **"이 코드가 대체 무슨 일을 하는 건지"** 감이 잘 안 올 수 있어요.  
이 파일에서는 금융 개념부터 코드 구조까지, **처음 보는 사람도 이해할 수 있게** 설명합니다.

---

## 1. 📌 프로젝트가 뭘 하는 건가요?

### 한 줄 요약
> 주식 5개 (`AAPL`, `MSFT`, `NVDA`, `SPY`, `QQQ`)로 구성된 포트폴리오를 $100,000 투자해서,  
> **1분마다 "오늘 최대 손실이 얼마일지"를 계산하고 비교하는 시스템**입니다.

### 왜 이게 중요한가요?

은행이나 펀드 회사는 매일 **"오늘 우리가 얼마나 잃을 수 있지?"** 를 계산해야 해요.  
이걸 **VaR (Value at Risk, 위험가치)** 라고 합니다.

- **99% VaR = $2,993** 이라면?  
  → *"오늘 하루 손실이 $2,993을 넘을 확률이 1% 미만이다"* 라는 뜻이에요.  
  → 100일 중 99일은 이 금액 이하로 손실이 난다는 의미입니다.

### 이 프로젝트의 핵심 질문

| 방법 | 속도 | 정확도 | 특징 |
|---|---|---|---|
| **Parametric VaR (Rolling)** | ⚡ 매우 빠름 (~0.2ms) | 보통 | 과거 252일 수익률 평균 사용 |
| **Parametric VaR (EWMA)** | ⚡ 매우 빠름 (~0.16ms) | 조금 더 좋음 | 최근 데이터에 더 가중치 부여 |
| **Monte Carlo VaR** | 🐢 상대적으로 느림 (~1.3ms) | 가장 유연함 | 10,000번 시뮬레이션 |

> **결론:** 빠를수록 가정이 많고, 정확할수록 계산이 오래 걸린다.  
> 이 트레이드오프를 숫자로 증명하는 것이 이 프로젝트의 목표입니다.

---

## 2. 💡 핵심 금융 개념 설명

### VaR (Value at Risk) 이란?

쉽게 말하면 **"최악의 경우 얼마 잃을 수 있어?"** 에 대한 통계적 답변입니다.

```
99% VaR = 2,993달러

→ "오늘 손실이 2,993달러를 넘을 확률 = 1%"
→ 즉, 100거래일 중 약 99일은 이보다 손실이 적다
```

### 공식이 어떻게 생겼나요?

**① 포트폴리오 가치 (매 분 업데이트)**
```
V_p = 주식A보유수량 × 현재가격A + 주식B보유수량 × 현재가격B + ...
```

**② 포트폴리오 변동성**
```
σ_p = √(w^T × Σ × w)
```
- `w` = 각 주식의 비중 (가격이 바뀌면 비중도 바뀜)
- `Σ` = 공분산 행렬 (주식들이 함께 움직이는 정도)

**③ Parametric VaR 계산**
```
VaR(99%) = 2.326 × σ_p × V_p
```
- `2.326` = 정규분포에서 99%에 해당하는 z-값

> 💡 **직관적 이해:** 포트폴리오 변동성이 크거나, 포트폴리오 규모가 클수록 VaR도 커집니다.

---

### 공분산 행렬 (Σ) 이란?

주식들이 "얼마나 함께 움직이는지" 를 수치화한 행렬이에요.

```
         AAPL   MSFT   NVDA   SPY    QQQ
AAPL  [  1.00   0.48   0.27   0.54   0.57 ]
MSFT  [  0.48   1.00   0.46   0.75   0.77 ]
NVDA  [  0.27   0.46   1.00   0.64   0.73 ]
SPY   [  0.54   0.75   0.64   1.00   0.94 ]
QQQ   [  0.57   0.77   0.73   0.94   1.00 ]
```

- SPY와 QQQ의 상관계수가 **0.94** → 거의 같이 움직임 (ETF니까 당연)
- NVDA와 AAPL이 **0.27** → 상대적으로 독립적으로 움직임

두 가지 추정 방법을 사용합니다:

**Rolling (동일 가중치):** 최근 252일 데이터를 똑같이 반영
```
오늘 ───────────────────── 252일 전
  모든 날을 똑같이 1/252 비중으로 평균
```

**EWMA (지수가중평균, λ=0.94):** 최근 데이터에 더 높은 가중치
```
오늘     어제     그제     3일전  ...
 6%      5.6%    5.3%    5.0%  ... (점점 줄어듦)
```
→ 시장이 갑자기 출렁이면 EWMA가 더 빠르게 반응합니다.

---

### Monte Carlo VaR 란?

**"만약 내일 주식이 이렇게 움직인다면?"** 을 10,000번 시뮬레이션해서  
그 손실 분포에서 99번째 백분위를 읽는 방법입니다.

```
시뮬레이션 1: AAPL -1.2%, MSFT +0.3%, NVDA -2.1% → 손실 $1,823
시뮬레이션 2: AAPL +0.8%, MSFT -1.5%, NVDA +0.9% → 이익 $412
시뮬레이션 3: ...
...
시뮬레이션 10,000: ...

→ 10,000개 손실값을 정렬 → 9,900번째 값 = VaR(99%)
```

수학적으로는 **GBM (기하 브라운 운동)** 모델을 사용합니다:
```
내일 가격 = 오늘 가격 × exp(상관관계를 반영한 랜덤 충격)
```

---

### Kupiec 테스트란?

**"우리 VaR 모델이 맞는지 틀리는지"** 를 통계적으로 검증하는 방법입니다.

- **VaR 위반 (breach):** 실제 손실 > 예측 VaR → 모델이 틀린 날
- 99% VaR이면 100일에 약 **1일** 위반이 정상
- 너무 많이 위반되면 모델이 위험을 과소평가하는 것

```
우리 결과:
- 관측일수 (T) = 498일
- 실제 위반 횟수 = 7번
- 기대 위반 횟수 = 4.98번 (~5번)
- Kupiec p-value = 0.39

→ p-value > 0.05 이므로 "모델 기각 안 됨" ✅
→ 우리 VaR 모델이 통계적으로 합리적이다!
```

---

## 3. 🗂️ 폴더 구조 설명

```
IE 420 Project/
│
├── 📄 config.py              ← 모든 설정값이 여기 있어요 (첫 번째로 볼 파일)
├── 📄 requirements.txt       ← 설치해야 할 파이썬 패키지 목록
├── 📄 run_pipeline.py        ← 전체 파이프라인 한 번에 실행하는 스크립트
├── 📄 README.md              ← 영어 설명서
├── 📄 친절한한국어설명.md     ← 지금 이 파일!
│
├── 📁 data/
│   ├── raw/                  ← 야후 파이낸스에서 다운받은 원본 데이터
│   │   ├── daily_prices.csv         (2022~2024 일별 주가)
│   │   └── intraday_1min_prices.csv (특정 하루의 1분봉 데이터)
│   │
│   └── processed/            ← 가공된 데이터
│       ├── daily_log_returns.csv    (로그 수익률)
│       ├── var_results.csv          (1분마다 계산된 VaR 결과)
│       ├── mc_var_results.csv       (Monte Carlo VaR 결과)
│       └── backtest_series.csv      (백테스팅 결과)
│
├── 📁 src/                   ← 핵심 파이썬 코드들
│   ├── data_loader.py        ← 데이터 다운로드/불러오기
│   ├── preprocess.py         ← 데이터 정제 및 로그수익률 계산
│   ├── portfolio.py          ← 포트폴리오 가치, 비중 계산
│   ├── covariance.py         ← Rolling/EWMA 공분산 추정
│   ├── parametric_var.py     ← Parametric VaR 계산 + 1분 단위 replay
│   ├── monte_carlo_var.py    ← Monte Carlo 시뮬레이션 VaR
│   ├── backtesting.py        ← VaR 위반 분석 + Kupiec 테스트
│   ├── latency.py            ← 계산 속도 측정 유틸리티
│   ├── dashboard.py          ← Plotly/Dash 대시보드
│   ├── report_generator.py   ← CSV 결과를 LaTeX 표로 변환
│   └── utils.py              ← 기타 헬퍼 함수들
│
├── 📁 notebooks/             ← Jupyter 노트북 (탐색/시각화용)
│   ├── 01_data_exploration.ipynb
│   ├── 02_parametric_var_baseline.ipynb
│   ├── 03_monte_carlo_var.ipynb
│   ├── 04_backtesting_analysis.ipynb
│   └── 05_dashboard_demo.ipynb
│
├── 📁 results/               ← 최종 결과물
│   ├── figures/              ← 그래프 PNG 파일들
│   └── tables/               ← 요약 CSV 파일들
│
└── 📁 report/                ← 최종 보고서
    ├── final_report.tex      ← LaTeX 보고서 원본
    ├── final_report.pdf      ← 컴파일된 PDF ✅
    ├── references.bib        ← 참고문헌
    ├── figures/              ← 보고서용 그림
    └── tables/               ← 보고서용 LaTeX 표
```

---

## 4. ⚙️ 각 소스 파일이 하는 일

### `config.py` — 설정의 중앙 허브
```python
TICKERS = ["AAPL", "MSFT", "NVDA", "SPY", "QQQ"]  # 투자 종목
INITIAL_CAPITAL = 100_000    # 초기 투자금 $100,000
COV_WINDOW = 252             # 공분산 추정에 쓸 과거 데이터 기간 (1년)
EWMA_LAMBDA = 0.94           # EWMA 감쇠 계수 (0에 가까울수록 최근 데이터 중시)
MC_PATHS = [1_000, 5_000, 10_000]  # Monte Carlo 시뮬레이션 경로 수
PRIMARY_CONFIDENCE = 0.99    # 주 VaR 신뢰수준 99%
```
> 이 파일만 바꾸면 전체 프로젝트 설정이 바뀝니다.

---

### `src/data_loader.py` — 데이터 수집
- `download_daily_prices()`: 야후 파이낸스에서 2022~2024년 일별 종가 다운로드
- `download_intraday_prices()`: 특정 날의 1분봉 데이터 다운로드
- `load_saved_data()`: 이미 저장된 CSV 불러오기

> ⚠️ yfinance는 1분봉 데이터를 최근 30일만 제공해요.  
> `config.py`의 `INTRADAY_DATE`를 최근 날짜로 설정해야 합니다.

---

### `src/portfolio.py` — 포트폴리오 관리
- 처음에 $100,000을 5개 종목에 **균등 배분** (각 $20,000씩)
- 주식 수량은 고정 → 가격이 변하면 **비중이 동적으로 변함**

```
예시) AAPL 시작가 $281.12 → 보유주식 = $20,000 / $281.12 = 71.1주
     이후 가격이 오르면 AAPL 비중이 다른 종목보다 커짐
```

---

### `src/covariance.py` — 리스크의 핵심
- `rolling_covariance()`: 최근 252일 데이터로 공분산 계산 (모든 날 동일 가중치)
- `ewma_covariance()`: 최근 데이터에 더 높은 가중치로 공분산 계산 (λ=0.94)
- `compare_covariance_matrices()`: 두 방법 비교 출력

---

### `src/parametric_var.py` — 빠른 VaR 계산
가장 핵심적인 파일입니다. 1분마다:
1. 현재 주가로 포트폴리오 가치 업데이트
2. 동적 비중 계산
3. 포트폴리오 변동성 계산 (σ_p)
4. VaR = z × σ_p × V_p

**실행 결과:**
```
평균 99% VaR (Rolling): $2,993  ← 하루 최대 예상 손실
평균 99% VaR (EWMA):   $2,489  ← EWMA가 더 낮게 나옴 (최근 변동성이 낮아서)
평균 계산 속도: ~0.2ms/분봉    ← 엄청 빠름!
```

---

### `src/monte_carlo_var.py` — 정교하지만 느린 VaR
```
경로수 1,000개  → VaR $3,021  (0.47ms)
경로수 5,000개  → VaR $2,829  (0.90ms)
경로수 10,000개 → VaR $2,936  (1.61ms)
```
경로 수가 많을수록 안정적인 결과가 나오지만 시간이 더 걸립니다.  
Parametric VaR(~0.2ms)보다 **약 7~8배 느리지만**, 1.6ms도 실용적으로 충분히 빠릅니다.

---

### `src/backtesting.py` — 모델 검증
- 매일의 실제 손실을 전날 VaR 예측과 비교
- **위반(breach):** 실제 손실 > VaR 예측값
- Kupiec 테스트로 위반 횟수가 통계적으로 적절한지 검증

**결과 해석:**
```
498일 관측 중 7번 위반
기대 위반 = 498 × 1% = 4.98번

7번 위반이 4.98번보다 많긴 하지만...
Kupiec p-value = 0.39 > 0.05 → "통계적으로 문제없음" ✅
```

---

## 5. 🚀 파이프라인 실행 방법

### 가장 쉬운 방법: 한 번에 전체 실행
```bash
cd "IE 420 Project"
python3 run_pipeline.py
```

이 명령어 하나로 아래 과정이 자동으로 실행됩니다:
1. ✅ 데이터 다운로드
2. ✅ 로그 수익률 계산
3. ✅ 포트폴리오 초기화
4. ✅ 공분산 추정 (Rolling + EWMA)
5. ✅ Parametric VaR 1분 replay (151개 봉)
6. ✅ Monte Carlo VaR 벤치마크
7. ✅ Monte Carlo VaR 1분 replay
8. ✅ 백테스팅 + Kupiec 테스트
9. ✅ 그래프 저장 (`results/figures/`)
10. ✅ 보고서 자산 생성 (`report/figures/`, `report/tables/`)

### 단계별 실행 (개별 테스트할 때)
```python
# 파이썬 인터프리터 또는 노트북에서
import sys; sys.path.insert(0, '.')

# 1. 데이터 불러오기
from src.data_loader import load_saved_data
import config
daily = load_saved_data('daily_prices.csv', config.DATA_RAW)

# 2. 로그 수익률
from src.preprocess import compute_log_returns
log_returns = compute_log_returns(daily)

# 3. 공분산
from src.covariance import rolling_covariance
cov = rolling_covariance(log_returns)
print(cov.round(6))
```

---

## 6. 📈 대시보드 사용법

```bash
python3 src/dashboard.py
```
→ 브라우저에서 `http://127.0.0.1:8050` 열기

**대시보드에서 볼 수 있는 것들:**

| 패널 | 내용 |
|---|---|
| 포트폴리오 가치 | 장중 분 단위 가치 변화 |
| VaR 비교 | Rolling, EWMA, Monte Carlo 세 방법 동시 비교 |
| P&L vs VaR | 실제 손익 vs VaR 한계선 (빨간 점 = 위반) |
| 지연시간 차트 | 방법별 계산 속도 비교 |
| 요약 테이블 | 평균 지연시간, 위반 횟수, Kupiec p-value |

---

## 7. 📝 PDF 보고서 컴파일

보고서는 이미 컴파일되어 있습니다: `report/final_report.pdf`

재컴파일이 필요하면:
```bash
# tectonic이 설치되어 있는 경우 (이미 설치됨)
cd report
~/.local/bin/tectonic final_report.tex
```

---

## 8. 📊 실제 결과 수치 정리

### 포트폴리오 초기화 (2026-05-01 기준)
| 종목 | 보유 주식 수 | 초기 비중 |
|---|---|---|
| AAPL | 71.07주 | 20% |
| MSFT | 48.34주 | 20% |
| NVDA | 99.22주 | 20% |
| SPY  | 27.73주 | 20% |
| QQQ  | 29.86주 | 20% |

### VaR 결과 요약
| 방법 | 평균 99% VaR | 평균 계산 속도 |
|---|---|---|
| Parametric (Rolling) | $2,993 | 0.19 ms |
| Parametric (EWMA) | $2,489 | 0.16 ms |
| Monte Carlo 1k | $3,021 | 0.47 ms |
| Monte Carlo 5k | $2,829 | 0.90 ms |
| Monte Carlo 10k | $2,936 | 1.61 ms |

### 백테스팅 결과
| 항목 | 수치 |
|---|---|
| 검증 기간 | 498 거래일 |
| 실제 위반 횟수 | 7번 |
| 기대 위반 횟수 | 4.98번 |
| Kupiec p-value | 0.39 |
| 모델 기각 여부 | 기각 안 됨 ✅ |

---

## 9. 🧩 자주 하는 질문

**Q. INTRADAY_DATE를 바꾸고 싶어요**  
A. `config.py`에서 `INTRADAY_DATE = "2026-05-01"` 부분을 최근 거래일로 변경하세요.  
yfinance는 최근 30일치 1분봉만 제공합니다.

**Q. 종목을 추가하고 싶어요**  
A. `config.py`의 `TICKERS` 리스트에 추가하면 됩니다.  
나머지 코드는 자동으로 적용됩니다.

**Q. Monte Carlo 경로 수를 바꾸고 싶어요**  
A. `config.py`의 `MC_PATHS = [1_000, 5_000, 10_000]`을 수정하세요.

**Q. 신뢰수준을 95%로만 보고 싶어요**  
A. `config.py`의 `PRIMARY_CONFIDENCE = 0.95`로 변경하세요.

**Q. 결과가 매번 같게 나오나요?**  
A. Monte Carlo는 랜덤이지만 `MC_RANDOM_SEED = 42`로 고정되어 있어서  
동일한 조건에서는 항상 같은 결과가 나옵니다.

---

## 10. 🔑 핵심 수식 한눈에 보기

```
포트폴리오 가치:       V = Σ(주식수 × 현재가격)
동적 비중:             w_i = (주식수_i × 가격_i) / V
포트폴리오 변동성:     σ_p = √(w^T Σ w)
Parametric VaR(99%):  VaR = 2.326 × σ_p × V
EWMA 공분산:          Σ_t = 0.94 × Σ_{t-1} + 0.06 × r_{t-1} × r_{t-1}^T
Monte Carlo:          내일 가격 = 오늘 가격 × exp(상관 충격)
Kupiec LR 통계량:     -2ln[p^x(1-p)^(T-x) / (x/T)^x(1-x/T)^(T-x)] ~ χ²(1)
```

---

*이 파일에서 이해가 안 되는 부분이 있으면 언제든지 질문하세요!* 😊
